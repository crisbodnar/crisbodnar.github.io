source 'https://rubygems.org'

# force Bundler to install & load the real URI gem (v1.0.3)
gem 'uri', '1.0.3', require: false

group :jekyll_plugins do
  gem 'github-pages'
end

group :other_plugins do
  gem 'httparty'
  gem 'feedjira'
end